import type { ReactElement } from "react"

export default function Index(): ReactElement {
  return (
    <section className="flex flex-1 items-center">
      <span className="text-gray-500">Pick a project from the list</span>
    </section>
  )
}
